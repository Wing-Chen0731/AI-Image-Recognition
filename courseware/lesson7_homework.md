# 第七课时课后作业：目标检测接入 Web 系统

本次作业围绕第七课的核心闭环展开：

```text
上传图片 -> Flask 接收请求 -> YOLOv8 推理 -> OpenCV 画框 -> 返回 JSON -> 前端展示结果
```

请同学完成后提交截图、简短文字说明和必要代码截图。重点不是只跑出结果，而是能说清楚“每一步是谁在做、为什么要这样做”。

## 一、基础概念题

### 题目 1：分类和检测的区别

请用自己的话解释：

1. 图像分类解决什么问题？
2. 目标检测解决什么问题？
3. 为什么第六课可以只返回 Top-3 类别，而第七课还要返回检测框坐标？

参考方向：

```text
分类：判断整张图是什么。
检测：判断图里有什么，并指出它在哪里。
```

### 题目 2：YOLO 和 OpenCV 的关系

请回答：

1. YOLOv8 在本项目里负责什么？
2. OpenCV 在本项目里负责什么？
3. 为什么有了 YOLO，还需要 OpenCV？

要求至少提到：

- YOLO 输出类别、置信度、检测框坐标。
- OpenCV 根据坐标画框、写文字、保存带框图片。

### 题目 3：置信度阈值是什么

请解释：

1. 什么是置信度？
2. 什么是检测阈值？
3. 阈值从 `0.3` 调到 `0.7` 后，检测框数量通常会发生什么变化？为什么？

## 二、代码理解题

### 题目 4：解释 `DetectionResult`

阅读 `app/object_detector.py`，回答：

1. `DetectionResult` 里面保存了哪些字段？
2. `x1, y1, x2, y2` 表示什么？
3. `to_dict()` 为什么要把 `confidence` 乘以 `100`？
4. 为什么后端返回给前端之前要把对象转成字典？

### 题目 5：解释 `/predict` 和 `/detect`

阅读 `app/web_app.py`，完成表格：

| 接口 | 对应功能 | 使用的模型 | 返回结果 |
|---|---|---|---|
| `/predict` |  |  |  |
| `/detect` |  |  |  |

要求说明：

- `/predict` 是第六课已有的分类接口。
- `/detect` 是第七课新增的检测接口。

### 题目 6：解释 `draw_detections()`

阅读 `app/preprocess.py`，回答：

1. `cv2.imread()` 做什么？
2. `cv2.rectangle()` 做什么？
3. `cv2.putText()` 做什么？
4. `cv2.imwrite()` 做什么？
5. 为什么这个函数返回的是 `target` 图片路径？

## 三、自我实操作业

### 实操 1：启动项目并完成分类测试

操作要求：

1. 激活 conda 环境。
2. 安装依赖。
3. 启动 Flask 项目。
4. 打开浏览器。
5. 上传一张猫或狗图片。
6. 截图保存分类 Top-3 结果。

需要提交：

- 终端启动成功截图。
- 浏览器分类结果截图。
- 简短说明：Top-1 类别是什么，概率是多少。

### 实操 2：完成目标检测测试

操作要求：

1. 点击页面中的“目标检测”。
2. 上传一张包含常见物体的图片，例如人、猫、狗、车、椅子。
3. 观察带框图片。
4. 截图保存检测结果。

需要提交：

- 目标检测页面截图。
- 检测结果中至少一个目标的类别、置信度和框坐标。

### 实操 3：调节阈值并观察变化

用同一张图片分别测试：

```text
0.3
0.5
0.7
```

填写表格：

| 阈值 | 检测框数量 | 观察结果 |
|---|---:|---|
| 0.3 |  |  |
| 0.5 |  |  |
| 0.7 |  |  |

要求回答：

1. 哪个阈值下检测框最多？
2. 哪个阈值下检测结果最严格？
3. 阈值太低可能有什么问题？
4. 阈值太高可能有什么问题？

### 实操 4：解释一次完整检测流程

请用 8-12 句话解释下面流程：

```text
用户上传图片
-> 前端调用 /detect
-> Flask 保存图片
-> YOLOv8 检测目标
-> OpenCV 画框
-> Flask 返回 JSON
-> 前端展示结果
```

要求必须出现这些关键词：

- `templates/index.html`
- `app/web_app.py`
- `app/object_detector.py`
- `app/preprocess.py`
- `YOLOv8Detector`
- `draw_detections`
- `JSON`

## 四、排错题

### 题目 7：看到 Flask warning 要不要解决

如果终端出现：

```text
WARNING: This is a development server. Do not use it in a production deployment.
```

请回答：

1. 这是不是代码错误？
2. 本地课堂运行需要解决吗？
3. 什么情况下才需要换生产服务器？

### 题目 8：找不到模型权重

如果浏览器显示 `Internal Server Error`，终端最后写着：

```text
No fine-tuned checkpoint found
```

请回答：

1. 这个问题和 YOLO 有关吗？
2. 它缺的是分类模型还是检测模型？
3. 应该检查哪两个文件路径？

提示：

```text
models/oxford_pet_mobilenet_epoch1.pth
finetuned_mobilenet.pth
```

### 题目 9：找不到 `ultralytics`

如果终端报：

```text
ModuleNotFoundError: No module named 'ultralytics'
```

请回答：

1. 这说明哪个依赖没有安装？
2. 应该先激活哪个环境？
3. 应该执行什么命令安装依赖？

## 五、拓展挑战

以下任选一题完成。

### 挑战 1：按置信度排序检测结果

目标：

让检测结果列表按照置信度从高到低显示。

提示：

- 可以在后端对 `detections` 排序。
- 也可以在前端对 `payload.detections` 排序。

提交：

- 修改前后截图。
- 修改了哪个文件、哪几行。
- 简短说明排序逻辑。

### 挑战 2：修改默认检测阈值

目标：

把默认检测阈值从 `0.5` 改成 `0.6`。

提示：

检查 `app/web_app.py`：

```python
DEFAULT_DETECTION_THRESHOLD = 0.5
```

提交：

- 修改后的代码截图。
- 页面默认阈值截图。
- 简短说明阈值变高后可能带来的影响。

### 挑战 3：给检测结果增加中文说明

目标：

在页面检测结果区域增加一句中文提示，例如：

```text
置信度越高，表示模型越确定该检测结果。
```

提交：

- 修改后的页面截图。
- 修改了 `templates/index.html` 的哪一部分。

## 六、提交清单

请最终提交：

- 分类运行截图 1 张。
- 检测运行截图 1 张。
- 阈值对比表 1 份。
- 概念题答案。
- 代码理解题答案。
- 排错题答案。
- 如完成拓展挑战，额外提交修改说明和截图。

## 七、评分建议

| 项目 | 分值 | 要求 |
|---|---:|---|
| 基础概念 | 20 | 能区分分类、检测、YOLO、OpenCV |
| 代码理解 | 25 | 能解释关键文件和函数作用 |
| 自我实操 | 30 | 能完整跑通分类和检测 |
| 排错能力 | 15 | 能根据报错定位模型、依赖、路径问题 |
| 拓展挑战 | 10 | 能做小修改并说明原因 |

